# Bimanual hardware replay

- User clarified the requested feature is bimanual hardware replay, not teleoperation. Replay currently explicitly rejects bimanual robots. Implement in isolated branch codex/bimanual-hardware-replay based on staging 13eade92, preserving the user's active checkout.
- Accepted design: select a saved bimanual robot and an episode; resolve both follower ports/calibrations server-side, validate family and complete left/right action layout before energizing, connect followers only, ease both to frame zero before starting a shared playback timeline, and retain cancellation/return/release handling for both. Support built-in SO-101, Maker and Metal via family configs. No leader connection is needed.
- Keep the existing flat prefixed telemetry contract; UI displays separate left/right readings and warns that both arms will move. Three mocked panel tests passed for bimanual launch, grouped readings/stop, and unchanged single-arm warning. Backend implementation and broader validation pending.
- No hardware operations, services, real dataset mutations, commit, push or PR publication authorized for this new feature. Validation uses mocked devices and temporary data only.
- Frontend validation: all 558 tests across 52 files passed, including localization parity and new replay panel cases; both TypeScript projects passed; lint reported 34 warnings and no errors. Production build validated in a temporary directory, leaving generated application bundles unchanged.
- User subsequently authorized committing and moving this feature to origin/staging. Fetched staging: its only change since the feature base is the CI frontend bundle rebuild at 09d7051d; preserve it during integration. No hardware or running-checkout change authorized.
- API snapshot regenerated; all 5 API contract tests passed. Backend implementer reported 223 replay/session/Maker tests passing; independent review ran 69 replay regressions. Review resolved partial CAN handshake recovery and independent per-arm disconnect cleanup. Final follow-up is clearing Metal velocity feedforward when one side reaches frame zero before its peer.
- Final implementation clears Metal velocity feedforward at the reached starting pose while waiting for its peer; hold failure cancels playback. Exact feature names are now checked before calibration staging. Implementer ran 156 relevant tests plus 28 focused tests on the final source; independent reviewer ran 75 replay regressions with the exact staging dependency. No remaining review findings. Changed-file pre-commit checks passed; OpenAPI export was run separately and verified by 5 API contract tests.
- Integrated the latest staging bundle commit by fast-forward to 09d7051d (no source changes or conflicts). Publication is authorized; commit/push follows this entry. Physical replay, timing, convergence and recovery remain untested; active checkout and running services remain untouched.
