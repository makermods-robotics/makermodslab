# PR 72 staging integration — 2026-09-11

- Original `239c03f9b34f1879fdcb9b8f9cc7fa85ff2f2167` contains PR71 plus two W&B commits. Merged updated parent `783da9280a09f688ed631dc91783d874755046c8`, including staging `359c7d1ee84e23837cf5843e8305e2fc0db0f39e`; retained parent history.
- Preserved current dataset and resume-owner handling, remote extras, localization and all newer tests while removing the obsolete W&B install gate. Added the credential probe as typed v1-only API, shrank retired legacy route ratchets, regenerated OpenAPI; added matching English/Chinese entity hints. Fixed old test doubles for current JobRecord and two-argument dataset upload contracts.
- Scoped jobs/train/preflight/system/API run: 529 passed, three integration-test failures subsequently fixed. Follow-up preflight/API/cloud run: 92 passed, one upload-stub mismatch fixed and its focused rerun passed; only existing installed-metadata-vs-checkout cloud-pin assertion remains (shared venv eaab6933 versus checkout 0efbaf55, covered independently by PR117). Evidence `/private/tmp/pr72-pytest-20260911.log`, `/private/tmp/pr72-pytest-followup-20260911.log`, `/private/tmp/pr72-cloud-secret-followup-20260911.log`.
- Both TypeScript projects, scoped ESLint, Ruff check/format and whitespace checks pass. Full pre-commit/full suite and real training/cloud/hardware validation unrun; no dependencies installed. No foreign worktree edits or publication.
- Publish #71 first; #72 retains its ancestor commits, so after a squash merge recheck its base diff before publishing/merging.

## Reviewer B1/B2 follow-up

- Credentials now follow execution location and effective W&B mode: LAN-origin submission defers to the peer, while local/HF-cloud online logging retains preflight validation. Offline/disabled modes neither probe W&B keys nor forward cloud W&B secrets. Resume copies the selected checkpoint owner's mode before validation, preserving online refusals even if a form requests offline.
- Moved all W&B-specific preflight tests from the offline-mode test file into `tests/test_wandb_preflight.py`, so PR75 can retire its offline tests independently. Added endpoint/registry and cloud-secret regressions; initial focused check passed 20 tests. Combined jobs/train/cloud/LAN/preflight/API checks running; known shared-env metadata assertion explicitly deselected (already covered by separate PR117 fix).
- Final combined check: **604 passed, 1 deselected** in 56.60s; evidence `/private/tmp/pr72-review-fixes-combined.log`. Deselection is only the known installed-metadata/checkout-pin environmental assertion. All new LAN/mode/inherited-mode cases pass, including attempts to bypass an online parent's key requirement. Scoped Ruff check/format and whitespace pass. No API schema change, hardware, cloud execution, services or dependency installs. Awaiting coordinator review; no publication.
