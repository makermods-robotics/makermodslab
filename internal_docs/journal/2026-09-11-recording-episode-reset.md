# Recording episode saving and follower reset

- User requested a new PR targeting main; PR #157 must remain unchanged. Integrated the recording fixes from detached staging checkout b7d56ee7 onto origin/main e4a14679 in an isolated worktree. Preserve main's dependency/preview architecture and exclude unrelated staging changes and generated frontend bundles.
- Timed episode completion saves the take instead of implicitly re-recording/discarding. Per-task capture keeps its pending take until the naming decision.
- Maker and SO-101 return non-gripper joints to calibrated zero after an episode, including re-record, and hold through the reset gap. Maker targets respect configured soft limits. SO normalized zero is converted through the calibrated bus. Completed home replaces the final cleanup target; failed home stops gracefully and preserves a completed take.
- Maker recording catch-up is 60 deg/s nominal (200% of original); home return matches SO-101's 400 raw steps/s, 400\*360/4096 = 35.15625 deg/s. This is nominal setpoint shaping, not physical-speed certification. Normal Maker recording has no continuous speed cap by default (`max_relative_target=None`).
- User explicitly reverted the leader-at-zero episode-start gate. It is excluded. Bounded pre-recording alignment remains required; failed alignment blocks capture. UI displays home/alignment progress.
- Hardware motion, calibration, actual home paths and actual speeds remain untested. No real datasets or devices used for validation. Main-pin Python validation and independent review in progress; frontend five tests and both TypeScript projects passed.
- Independent pre-publication review found B1: SO-101's pre-recording approach sampled a stale target once, permitting a jump if the leader moved before capture. B2: Maker's already-near-target shortcut could report home without replacing an outstanding recording goal. Fixes and focused regression coverage requested before publication; no hardware reproduction claimed.
- Integration validation before those review fixes: 198 targeted tests passed against main's exact LeRobot pin 5ce2fe41; frontend five tests, both TypeScript projects, lint (34 warnings, no errors), and temporary production build passed. Final revision must be rechecked after review fixes.
- Superseding user scope: remove all SO-101-specific motion changes from the PR. Keep SO-101's existing reset/passthrough and stop behavior, with no new home opt-in or preparation motion helper. Shared duration-saving correction remains. Maker home/reset and 60 deg/s catch-up remain. B1 is removed with the SO-specific approach; B2 still requires a Maker fix and re-review. The user's active checkout is preserved separately from this PR branch.
- Superseding scope extension: user also requests Metal. Add Metal zero return/reset hold and 60 deg/s catch-up while retaining SO-101 motion behavior. Metal calibration defines zero as upright, so preserve its captured resting-pose teardown (including powered leader targets) before torque release; only Maker treats completed folded home as its final resting target. Metal-specific tests and review pending.
- Prior Maker-only revision passed 499 main-pin tests and independent re-review, including the B2 fix. All-file pre-commit checks passed except the Markdown formatter updating the new journal; OpenAPI export ran separately with the exact main pin and reported up-to-date. Final Metal revision needs refreshed validation.
- Metal review found B3: the pinned driver smooths velocity feedforward and can retain nonzero desired velocity in the last home command while reset sends nothing. Added fix request for a Metal-specific final home hold that resets feedforward history and sends the same non-gripper home target with zero velocity, retaining gains. Regression will use the actual pinned driver with a mocked bus; physical motion remains unrun.
- Final Metal revision: 507 mocked tests passed against main's exact LeRobot pin (`/tmp/main-metal-extension-broad.log`); targeted suite 191 passed and independent review suite 24 passed. B1 removed with SO scope, B2 fixed by ensuring a home command is installed, B3 fixed by a Metal-specific zero-velocity final hold. Review found no remaining blockers. SO motion unchanged; no leader-home start gate.
- All-file pre-commit passed (OpenAPI hook run separately with the main pin: already up to date). Frontend five tests, both TypeScript projects, lint and temporary build passed. No generated bundles included. Hardware, service restart and real dataset operations remain unrun. New PR publication authorized; PR #157 and the active staging checkout remain untouched.
