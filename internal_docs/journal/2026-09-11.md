# 2026-09-11

## PR #125 staging integration

- Merged staging `359c7d1e` into PR head `0fda57a5` in an isolated checkout. The sole conflict appended independent test groups; preserved both camera re-anchoring and staging calibration-library tests.
- Fixed review B1: inference now selects policy-bound cameras before resolving their identities. An absent unused saved camera no longer blocks a policy; recording still checks all saved cameras. Added a regression covering unused versus selected absent cameras and recording refusal.
- Scoped validation: 628 tests passed across test_utils_config, test_record, test_rollout and test_camera_identity; Ruff check passed on all seven PR Python files and format check on the two edited Python files. Initial regression run caught a policy-name error label; retained saved camera names during resolution and reran successfully. Logs: `/private/tmp/pr125-pytest-20260911.log`, `/private/tmp/pr125-pytest-rerun-20260911.log`. No hardware or experiment execution.
