# PR 76 staging integration — 2026-09-11

- Original `c9409faeb4c5798a350e713f1f96f7da3785d395`; staging `5b4d3c4d88785c112937e124d2f317e6c636db40`. Isolated branch `codex/staging-pr76-20260911`.
- Preserved staging import-time app-state/output-root redirects, plus PR isolation of cache/pin lists and imported path copies. Deferred config imports until after redirects; a top-level import from the old PR would freeze real paths too early. Removed server path-copy entries because staging now resolves those paths at call time. Whoami stubs accept newer keyword arguments. Retained fake-subprocess stray-calibration cleanup tests.
- Initial fixture check exposed retired server constants before test bodies; fixed registry to current imports. Final 476 auto-calibration/model/dataset/auth tests pass, including read-only real-calibration canary unchanged; Ruff check/format pass. Evidence `/private/tmp/pr76-pytest-final-20260911.log`; earlier diagnostics `/private/tmp/pr76-pytest-20260911.log`.
- No hardware, real calibration writes, cloud jobs, experiments, services or dependency installs. Full suite and full pre-commit unrun; broader fixture consumers should run in CI.

## CI fixture follow-up

- CI run `34642286225` reported 3 failures, 3958 passes, and 10 skips. Reproduced the same three failures locally with the recording-preparation and state-home files: 3 failed, 23 passed (`/private/tmp/pr76-fixture-repro.log`; downloaded CI evidence `/private/tmp/pr76-ci34642286225.log`).
- Cause: the autouse fixture created `isolated_user_state` inside each test-owned `tmp_path`, violating meaningful empty-output assertions, and placed saved-list constants outside the configured application home, violating the state-home contract.
- Fix: allocate per-test cache storage with `tmp_path_factory` independently of test output directories, and keep unique saved-list storage beneath the already isolated `MAKERMODSLAB_HOME`. Production code, recording artifact assertions, and state-home assertions are unchanged; real user-state isolation and the calibration canary remain in place.
- Combined recording preparation, state home, auto-calibration, models, datasets, and HF-auth checks: 502 passed, 11 warnings (`/private/tmp/pr76-fixture-fixed.log`). Ruff check/format and diff whitespace checks pass. Full suite remains unrun locally; coordinator should review and publish for CI. No hardware, services, experiments, real user-state writes, or dependency installs.
