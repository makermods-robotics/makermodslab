# 2026-09-11 — PR #108 staging integration and older queue targets

- Authorized metadata changes: retargeted #71, #72, #74, #75, #76, #77, #78 to `staging`; GitHub verification confirmed unchanged heads. #77 is mergeable/clean; the other six conflict. Existing head checks are green on #71/#75/#76/#77/#78; #72/#74 expose none. These are historical checks, not newly run integration checks.
- Preserve stack order #71 before #72: verified #71 head `4461c47dcccc9983f4eab4205f27811f2728c5b6` is an ancestor of #72 head `239c03f9b34f1879fdcb9b8f9cc7fa85ff2f2167`. No branch rewrite or parent removal.
- PR #108 isolated checkout starts at `a2d3657dd6ef35299fe13a349fa26943fa2e6241`; merge target is staging `359c7d1ee84e23837cf5843e8305e2fc0db0f39e`. Seven content conflicts; four existing rerere resolutions inspected against staging. Keep staging's current replay stop behavior and recording controls.
- Integration decision: carry teleop affordance into the extracted RobotControls; retain staging's single Deploy Start and top-packed Collect layout, applying robot-action treatment there. Do not restore removed RunVerbs or duplicate dialogs in RobotCorner.
- Initial validation: application TypeScript passed; targeted ESLint passed with two preexisting cleanup-ref warnings. Robot-action/catalog/key-usage tests: 14 pass, one action parity failure because staging added three remote session kinds. Raised this integration scope to coordinator before extending the action map or weakening its invariant.
- No services, hardware, experiments, shared dependency installations, push, or PR merge. Frontend tools reuse the existing dependencies through a worktree-local ignored symlink. An initial symlink command used the wrong relative path and created no file; corrected before validation.
- Follow-up validation: both newly merged component suites initially failed because their isolated renders omitted TooltipProvider required by the new wrappers. Added the application-equivalent provider to test fixtures; all 12 component tests pass (diagnostics `/private/tmp/pr108-component-tests.log`). Lint of all 24 changed TypeScript files passes with five warnings, no errors.
- Inspection found the inherited dataset-card Enter/Space handler also intercepts key events bubbling from its nested View button. Reported to coordinator for a narrow target/currentTarget guard and interaction test; not yet changed.
- Coordinator approved remote integration and the dataset keyboard fix. Added remote session-kind map entries and bilingual tooltips, preserving hosting's parked/remote-availability semantics. Marked actual station hosting/stop, remote teleop start/home/engage/stop, remote inference stop, and the Deploy remote start path. GPU-launch and navigation controls remain ordinary controls.
- Fixed dataset-card keyboard handling to ignore nested button events; four regression cases cover Enter/Space on the card and View button. Test harness corrections (required dataset fields, grid items prop, exact button name) completed before the final run.
- Final focused run: six suites, 31 tests passed, including action/OpenAPI parity, catalog/key checks, dataset keyboard regression and recording/calibration component tests (`/private/tmp/pr108-final-tests.log`). Both TypeScript projects pass. Changed TypeScript ESLint passes with five existing warnings and no errors. Diff whitespace and conflict-marker checks pass. No browser layout or hardware validation performed; no full suite, dependency installation or frontend bundle generation.
