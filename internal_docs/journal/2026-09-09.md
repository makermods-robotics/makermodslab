# 2026-09-09

## Metal gripper soft squeeze limit

- User authorized a soft gripper-only limit for sustained squeezing, with firmware/current limiting deferred. Implemented in isolated codex/metal-gripper-soft-limit checkout, preserving the original dirty project and PR109 branch.
- Robot settings expose nullable gripper_closing_error_deg, disabled by default and requiring an explicit finite positive value. Capability-gated to Metal, English/Chinese UI, applies to next local teleop/record session and both grippers for bimanual rigs. Persistence validates before writes, null clears, omitted preserves, arm-family switch clears.
- Per-follower bus adapter clips closing target error after stock command shaping; opening preserved. Zero gripper velocity/torque feedforward; fresh response decode with recent host-receipt timestamp. Missing/malformed/nonfinite feedback raises before affected-arm writes. This is a soft position-error limit, not guaranteed torque/current or jaw force. Extra feedback round trip adds unmeasured latency; independent bimanual bus scheduling and existing cleanup paths retained.
- Legacy named starts resolve saved settings; explicit null disables. Recorder saves limited gripper commands instead of unreachable leader targets, preserving other action fields. No pinned dependency edits.
- Validation: 219 runtime/record/teleop tests, 135 config tests, 152 registry tests, 84 session/API tests, and 17 frontend tests passed. Both TypeScript projects passed. Static checks rerun after formatter correction; frontend lint has baseline34 warnings and no errors. Independent review found no remaining blockers. OpenAPI snapshot regenerated and contract checked. No hardware, timing benchmark, commit, push, or merge.
- Hardware table identifies J7 DM4310 V7018 with Metal protocol scales6.28/30/20. Manufacturer firmware notes indicate standard revision18 supports force-position current saturation; actual installed subversion, Imax and mode/readback remain unverified. Soft implementation does not change firmware/control mode or TMAX.

## Experimental DM4310 current-limited position control

- User requested adding motor-enforced current limiting alongside the soft option, on the same branch. Added paired current ratio and maximum velocity settings, default disabled, no assumed safe preset. Local Metal teleop/record only; both grippers when bimanual.
- Implemented force-position mode4 gripper commands with firmware ASCII7018, Metal METAL_JHI scales and ID checks, mode readback, verified motor replies, current/speed quantization down, and an initial hold at sampled jaws. Other arm joints retain MIT commands. Confirmed disable precedes volatile mode restoration; no firmware flash or parameter save. Ordinary starts reject leftover mode4 after crash rather than silently sending incompatible MIT frames.
- Primary manufacturer protocol requires enable before control, and does not establish current limiting in the enable-to-first-command interval. UI and documentation explicitly label this experimental and hardware-unvalidated; current/Imax is not measured jaw force. No claim of a startup ceiling.
- Review corrected missing-joint startup verification and the actual Metal motor-profile enum. Final review found no remaining code blockers. Validation:391 config/registry/session/API tests,239 runtime tests,19 frontend tests passed; runtime uses actual pinned Metal follower/bus with fake CAN transport. Both TypeScript projects and backend static checks passed. Frontend lint remains34 existing warnings,0 errors. API snapshot regenerated and verified. No hardware, services, firmware modification, or benchmark executed.
