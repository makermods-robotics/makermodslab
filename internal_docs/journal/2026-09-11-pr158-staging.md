# PR #158 staging integration

- User authorized applying PR #158 (91d589ab) directly to origin/staging. Base b6edfffc, isolated worktree; apply as one integration commit and leave the main-targeting PR open.
- Preserve staging's preview/processor architecture and LeRobot pin while integrating timed-episode saving and Maker/Metal zero return/reset hold, 60 deg/s catch-up, ensured home command, Metal zero-velocity hold and captured resting-pose shutdown. SO motion unchanged; no leader-at-home start gate.
- Recording source/tests required conflict resolution due to the main/staging preview differences. Exact staging-pin validation and independent integration review pending. No active checkout, hardware, service or real dataset changes.
- Final integration validation: 214 targeted tests passed; broader suite 522 passed with one pre-existing registry guard failure at unchanged maker_ports.py:382 (present in base b6edfffc), evidence /tmp/staging-158-broad.log. Independent reviewer ran 26 focused tests and found no blockers; preview wrapper/default processor identities and cleanup restoration were preserved.
- Frontend five tests, both TypeScript projects, lint and temporary build passed. OpenAPI export against the exact staging pin was unchanged. All-file pre-commit checks passed except formatting a pre-existing PR154/156 journal and a spelling false positive inside a SHA in the existing frame-copies journal; preserved that unrelated file and retained the formatting diff in /tmp/pr158-preexisting-journal-format.patch. All changed-file hooks passed separately before publication.
