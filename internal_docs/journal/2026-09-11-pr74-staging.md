# PR 74 staging integration — 2026-09-11

- Original `551289e1480a606ebfa0e3732987abc64c593cc2`; fresh staging `5b4d3c4d` (full base available as merge parent). Isolated branch `codex/staging-pr74-20260911`.
- Much of the old PR already exists on staging: identity enumeration/pump, frontend retries, experiment script, and server startup/shutdown wiring. Kept those staging implementations; removed the auto-merge duplicate pump rather than registering it twice. Remaining change bounds preview reads and lock contention, retains per-device wedge tombstones, and adds fake-capture regressions. Preserved staging 640x480 preview negotiation and hosting guards.
- 52 scoped camera identity/preview tests pass; Ruff check/format and whitespace checks pass. Evidence `/private/tmp/pr74-pytest-20260911.log`. Frontend and API are unchanged versus staging, so no rebuild or API regeneration needed.
- No hardware, experiment script, services or camera devices run. Unit tests use fake captures. Wedge recovery remains a process restart; per-read worker cannot interrupt native reads.
- PR125 commits were not mixed in: this PR changes camera_identity documentation only and camera_preview containment, preserving staging capture configuration. Recheck combined diff when PR125 lands. Full suite/pre-commit unrun.
