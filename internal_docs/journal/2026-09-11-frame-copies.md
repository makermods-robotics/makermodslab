# Recording frame copy removal — 2026-09-11

- User authorized publishing the LeRobot fix and opening a PR to MakerModsLab main, without merging. Isolated Lab branch starts from main e4a14679.
- Published dependency revision f33ec84bc075eae153f9b1dff0ee74076e752dbd on Mokuroh54/lerobot, branch codex/recording-frame-copies-pinned. Its parent is the prior Lab pin 5ce2fe41a63647c20b2ab6e84bcb4cc7295a9b21, preserving existing Metal/Maker changes. Push to the organization fork was denied (403); the existing personal fork is writable and contains the published revision. Both base and optional metal-leader dependency pins now use that exact revision.
- Dependency removes RGB ndarray-to-PIL-to-PyAV conversion and shares freshly allocated read-only OpenCV frames only within default recording. Unknown/mutable buffers retain snapshots; policy/custom processing/display remain outside the sharing path. Scope restoration preserves mutable later camera reads.
- Main's prepared-encoder worker overrides the dependency worker, so also removed its PIL round-trip locally. Tests verify real PyAV conversion of HWC/CHW/strided/float inputs, RGB contents, timestamps, readiness and statistics; depth behavior retained.
- Validation: dependency ownership/conversion suite 20 passed; combined Lab recording/preparation suite against the new dependency source 175 passed (11 existing deprecation warnings), scoped Ruff checks passed. Regenerated uv.lock resolves 177 packages with only the LeRobot source change; offline lock check passes. Reviewed actual dependency and Lab diffs. No live recording, hardware operations, actual encoding, performance benchmarks, or full environment installation performed; no measured speedup claimed.
- PR publication pending this entry; no merge performed. Current running installation and unrelated primary checkout edits remain unchanged.
- User clarified preference for the company-owned LeRobot dependency. Restored original organization URL and exact 5ce2fe41a pin; the historical personal-fork publication above does not describe the updated PR. Queue-sharing activation remains pending company-fork integration.
- User authorized automated viewer detection and shared JPEG preview caching at unchanged recording FPS/resolution. Implementing per-camera recent-request leases and per-frame single-flight JPEG caching in HTTP workers, no image sampling without active demand; retain joint telemetry independently. Preserve default observation processor identity by relocating the read-only tap, correcting the discovered integration mismatch. Validation pending.
- Implemented demand-driven preview: each camera request renews a two-second lease; no image copies without demand. JPEG results are cached per sampled frame and shared through per-camera HTTP encoding locks, with stop/start invalidation. No new background workers; recording resolution/FPS and independent joint telemetry cadence preserved.
- Restored original robot observation method on recording success/error and pass all three default processor pipelines unchanged; regression covers the actual Lab entrypoint. This fixes future sharing eligibility but does not claim encoder queue sharing on the unchanged company dependency.
- Validation of final preview implementation: agent ran 171 recording/preview tests against company 5ce2fe41a, all passed; scoped lint/format passed. Coordinator reviewed source/test diff and verified company pin matches base; 25 DRTC dependency-consistency tests passed, resolving prior CI failures caused by the personal-fork URL. Prepared-encoder regressions checked separately. No measured bandwidth or timing improvement.
- User authorized trying PyAV buffer wrapping in the prepared encoder. Keep the company dependency pin and camera queue safety snapshot; wrap compatible RGB uint8 arrays to remove the subsequent NumPy-to-PyAV copy, with copying fallback for unavailable API or incompatible layout. Validate backing storage lifetime and actual queue producer-reuse protection using fake encoders/real frame conversion; no performance experiment authorized or run. Prior PR153 head e86614ea has all three CI checks green; new implementation validation pending.
- Implemented packed RGB uint8 buffer wrapping via PyAV from_numpy_buffer, fallback to from_ndarray for other layouts or missing API; unexpected constructor errors propagate. Confirmed real company queue copies producer buffers before the prepared worker, preserving camera reuse isolation. PyAV v15.1.0 stores \_np_buffer; FFmpeg n7.1 avcodec_send_frame documents caller ownership with reference-or-copy input handling, so delayed encoding does not borrow freed input storage. Evidence: https://github.com/PyAV-Org/PyAV/blob/v15.1.0/av/video/frame.py and https://github.com/FFmpeg/FFmpeg/blob/n7.1/libavcodec/avcodec.h . Coordinator reviewed source/test diff; final targeted validation pending.
- Final buffer-sharing checks: 21 prepared-encoder tests passed against company LeRobot 5ce2fe41a; scoped Ruff lint/format and whitespace passed. Real frame conversions verify pointer identity, buffer lifetime after local reference release, producer mutation isolation, pixels/timestamps/stats, unsupported layouts and missing API fallbacks. No actual encoding or speed measurement; ready to publish to existing PR153.
- User authorized avoiding replacement of unconsumed preview snapshots. Plan one pending raw snapshot per demanded camera; publication skips copies while it awaits encoding and permits a new sample after consumption. JPEG caching/single-flight and recording FPS/resolution remain unchanged. Validate error recovery, demand expiry/restart and multiple viewers; slightly older pending preview acceptable as explained to user. No benchmark or hardware run authorized.
- Implemented one pending raw snapshot per camera: no replacement copies before consumption; successful encoding caches JPEG bytes and clears the slot. Failed or empty encoding drops the snapshot, allowing a later sample to recover. Ten preview tests passed, including per-camera copy counts, publication during concurrent encode, demand/session lifecycle, and failure recovery; scoped Ruff lint/format and whitespace checks passed. Coordinator reviewed source/test diff. Previous CI code/tests passed; its journal-only Markdown formatting failure corrected with the configured Prettier version. No benchmark or hardware run.
- User authorized direct squash of PR153 branch into staging while keeping PR153 open targeting main, declining a separate scoped staging PR after being told direct integration includes inherited main changes. Integration base f4ae38ad, source79ef3e4a. Resolved generated frontend conflicts by retaining staging bundle for CI rebuild; preserved Maker trigger-grip and incoming Metal vertical-grip options in the test conflict. Staging company LeRobot pin remains0efbaf55be27cfab8cfd0d29afe2f8aeac0afdb9.
- Independent review of18 nongenerated staged files found no blockers. Initial test run exposed network/cache isolation issues; offline temporary-cache run passed319 tests with6 vertical calibration-path failures. Added existing temporary-calibration fixture to that test and reran its entire file:14 passed, covering all325 unique tests across runs. Six frontend tests, both TypeScript configurations, scoped Ruff and staged whitespace checks passed. Generated bundle not rebuilt locally; no hardware, real video encoding, or benchmark run.
