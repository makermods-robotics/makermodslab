# Project journal — 2026-09-05

## Remote inference runtime excludes setup

- Verified that the displayed `elapsed_s` measured time since session launch, while both robot players' duration limits began at connection. The players excluded ease-in duration but still consumed the first-action wait from the requested run duration.
- Added explicit RUNNING and STOPPING events to both players. Execution starts after the first-action ease completes, immediately before the first normal policy command. Received chunks alone no longer enter running. A shared monotonic duration timer gives both engines the full requested execution duration; telemetry time also excludes setup.
- The parent now reports zero elapsed during setup/easing and measures execution from RUNNING. Stops, failures, and teardown freeze the elapsed value; session launch time remains the session identifier. Updated API/frontend contract comments without changing the status shape.
- Validation: 165 focused remote inference/API, protocol, player safety, and timer tests passed. Ruff checks, formatting, and diff whitespace checks passed. Timer tests cover long setup, repeated start, bounded/unbounded duration, and frozen terminal time. No live hardware session or GPU was launched/restarted; live verification remains for the next user-run session after restarting MakerModsLab. This change does not require a Modal GPU restart.

## GPU restart requests its replacement automatically

- Verified the existing Restart button awaited a stop request that returned `stopping`, then immediately attempted Start; the hook also swallowed stop failures. This raced asynchronous teardown and could leave the GPU stopped without a replacement.
- Restart now snapshots selected launch settings, waits for confirmed idle with a 45-second shutdown bound, and submits one replacement request. It keeps a visible pending state, serializes overlapping clicks, preserves ordinary stop-only behavior, and distinguishes shutdown failures from replacement launch failures.
- The launcher now keeps `stopping` through remote Modal cleanup even when local client EOF arrives first. Failed or unconfirmed cleanup produces an actionable failed status and retains the app record, preventing automatic replacement after an unconfirmed stop. Repeated Stop requests share existing cleanup.
- Validation: 136 focused launcher tests and 15 frontend selector/restart tests passed; both TypeScript checks, touched-file ESLint, Python Ruff checks/formatting, and diff whitespace checks passed. Tests use mocked HTTP/processes, including teardown races, failure/timeout paths, settings snapshots, and repeated clicks. Existing deprecation warnings only. No live GPU, service, or robot was started/stopped/restarted. Production frontend bundle still needs rebuilding after coordinator review.
- After coordinator review, rebuilt the production frontend successfully (`npm run build`): `index-a8K2sbz_.js` and unchanged `index-COxgF5Qw.css`; normal non-dev mode now includes restart behavior. Only the existing large-bundle advisory remains. Confirmed the user's `frontend/dist/so-101-urdf/CMakeLists.txt` bytes were unchanged before/after build (SHA-256 `51d7c851cdd0944f2b95994e9c3c18cc1c98a1edfc2cd849976cdf5d8dd599cc`). No live services or GPUs were touched.
