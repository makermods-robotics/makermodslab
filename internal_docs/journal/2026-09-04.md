# Project journal — 2026-09-04

## First instrumented Modal startup result

- Instrumented E2E follow-up: 33 steady sampled GPU log lines (t>=65 s in `1788584408`) have median build/pre/infer/post/total of 3.0/19.3/533.6/6.3/563.1 ms. These are sampled log medians, not per-request E2E decomposition. Robot session `1788584627` ended its last active sample at 1208.87/1302.78 ms p50/p95. Both camera buffers update at approximately 30 Hz with late-session ages about 4–33 ms and 640x480 images. Model computation explains a substantial component, but remaining observation queue/transport delay is not isolated; negative cross-host return readings remain unsuitable for subtractive attribution. Different camera types alone are not established as the cause.

- GPU log `1788584408.log` now supplies measured stage times: Tailscale startup 1.0 s; policy.from_pretrained 144.7 s; transfer to CUDA 2.5 s; input/output processors 1.8 s; warm-up 5.9 s; station TCP dial 0.281 s. This establishes model construction/loading as the dominant wait in this run, not Tailscale peer discovery. The specific sub-operation inside from_pretrained remains unprofiled.
- Actual loaded parameter inspection reported all 5,442,196,208 parameter elements as torch.bfloat16 on cuda:0. This confirms stored parameter precision/device for that run; it does not imply all intermediate operations use BF16.

## Remote run stuck in Setting up while moving

- Live status for the home RTC session reported phase `easing` after 53 logged seconds and 85 received chunks. Session log `1788584627` established the order: first positive STATS sample, then EASING, then completed first-action easing and continued chunks. The parent entered running only on its first counted chunk; subsequent easing overwrote that phase permanently.
- Fixed positive post-easing telemetry to restore running without resetting the original running timestamp. Phase updates are applied under the state lock; stopping/error phases are preserved. Late EASING events also cannot overwrite stopping/error. Regression tests cover the observed event order and stop/error preservation.
- UI now labels the easing phase explicitly instead of the generic Setting up badge. Rebuilt the production bundle from current source for normal-mode use, retaining all pre-existing source changes. No arm-motion settings, live session state or running Lab process were changed by this fix.
- Validation: 100 remote-inference/API tests passed; Ruff and diff checks passed; production frontend build succeeded. App and node TypeScript checks completed separately. Runtime verification requires a fresh Lab process after the user stops the active session.

## Normal-mode browser opening with --bind (21:58)

- User localized the apparent startup pause to after `Uvicorn running` and then identified the missing automatic browser opening at the new tailnet address. That first line indicates the listener is ready; a later remote `/health` request is not a startup stage.
- Verified normal mode serves the existing frontend bundle and skips npm. A separate import-only profile completed in 3.16 seconds (2.745 seconds cumulative server import, mostly LeRobot); this does not measure intermittent SFU/STUN or page-load delays.
- Fixed the launcher to auto-open the actual `--bind` address for desktop production launches. Readiness polling uses that same address, with IPv6 URL brackets and wildcard-to-loopback handling. Explicit `--lan` and `--no-ui` still suppress browser opening. Existing localhost and development-mode behavior retained.
- Verified tailnet, localhost and IPv6 browser targets with mocked sockets/browser opening; launcher checks: 43 passed in sandbox and the three socket-binding checks passed with localhost permission after sandbox denial. No running Lab process was restarted; change applies on next launch. Existing concurrent frontend bundle changes were preserved.

## DRTC diagnostics implementation and Modal resource check (21:48–21:55)

- Added RTC startup-stage timers and actual parameter dtype/device counts; per-chunk CPU build, preprocessing, synchronized prediction, postprocessing/transfer and total times; and Portal synchronization/buffering metrics. CUDA synchronization introduces possible measurement overhead, documented for like-for-like comparisons.
- Added a bounded camera-buffer monitor to the RTC robot process. It reads existing buffers with non-blocking locks and reports dimensions, post-driver-read age and observed update rate (a polling lower bound, not sensor FPS or exposure age). Unsupported cameras report unavailable. It never opens a camera or commands motors. Existing wire fields and run defaults remain unchanged.
- Both Modal wrappers now distinguish SOCKS-listener, login, total Tailscale startup, and actual station TCP-dial timing. No authentication or credential behavior was changed. Saved latest successful log shows Tailscale Running at 21:31:45 and policy signalling only at 21:34:35; a different run incurred the existing 20-second saved-identity fallback. The long model startup must not be described as Tailscale discovery delay.
- Added `docs/drtc/TIMING_DIAGNOSTICS.md` with timing limits and a matched-run recipe (including minimum budget 1 on both peers to match the earlier session). No matched live run was performed: Lab was no longer listening by the end of the audit, and a new GPU/session must load the instrumentation.
- Live Modal inspection at approximately 21:50: configured environment returned no running containers; all five recent DRTC apps stopped. Most recent `ap-3zEQdpwzoLJNJ0a0BWyp7e` stopped at 21:48:49. No resource was terminated by this task; other profiles/environments were not audited.
- Validation: 71 focused diagnostics/wrapper/robot-teardown/protocol tests passed; changed Python files passed Ruff and compilation. Four diagnostic tests passed again after the monitor shutdown guard was added. No GPU inference or physical-robot validation was run. Concurrent frontend build artifacts appeared in the working tree and were left untouched.

## DRTC camera and latency comparison (evening)

- Read earlier GPU/session logs (`1788553969` / `1788554220`) and current logs (`1788582687` / `1788582918`, `1788582978`). Verified current hardware identifies as H100 80GB and the loader receives BF16; effective tensor precision was not inspected. Earlier run also used BF16. No policy/runtime settings were changed during this audit.
- Earlier final latency sample: 631/1140 ms p50/p95 after 29 logged seconds; newer samples: 1108/2058 ms after 21 seconds and 1180/1457 ms after 5 seconds. These short, unequal windows are not a controlled performance benchmark. Robot emit work spans 7–13 ms earlier versus 5–14 ms and 7–10 ms now, with no logged loop overruns or stale-observation timeout warnings.
- Both GPU runs declare cam0/cam1, horizon 30, RTC enabled, slack 5, tolerance 1.5. Robot s_min changed from 1 to 4. This changes pacing/RTC overlap; its contribution to latency was not isolated. Current GPU sync warnings report video up to 600 ms ahead of unmatched states; negative cross-host return times (~-110 ms) invalidate a literal return-latency interpretation. Neither proves the full E2E cause. The RTC GPU logger lacks separate inference/preparation timing.
- Saved home and earlier booth camera records request 640x480 at 30 fps. Live enumeration matched both current external USB devices to the home camera identities. With no active session, read one existing Lab preview frame per external camera: both are 640x480, with distinct wrist/front views and visible grain/dim regions. Checkpoint cache expects 224x224 per view; GPU batch preparation resizes inputs. MJPG-setting fallback warnings occur in both historical and current session logs, so are not newly introduced evidence.
- Direct camera-only cadence measurement could not open cameras because this process lacks macOS camera authorization. Used the Lab's existing preview endpoint for snapshots instead; its 15 fps cap cannot establish true 30 fps recording delivery. No motor operations, session/GPU starts, configuration changes, or dependency changes were performed. Earlier footage and per-camera capture timestamps are unavailable, so equivalent recording quality/freshness is NOT established.
- Next: collect actual capture cadence/frame age through an authorized camera process and GPU inference/preparation timings; compare equal-duration runs with matched RTC settings before attributing the increase to camera type or GPU precision. Camera dependency publication remains parked.

Local date: America/Los_Angeles. This entry records the established context from the branch-cleanup task; it is not a history of all project activity today.

## Logging convention

Keep dated entries here for substantive user-directed work and decisions. Distinguish verified findings from proposals, record validation and blockers, and update alongside that work rather than through scheduled background activity. No existing daily-journal convention was found under `internal_docs/`.

## Decisions and completed work

- Refreshed origin refs and checked open GitHub PRs before assessing branches. Branch age alone was not treated as proof that work could be discarded.
- Deleted 13 dormant origin branches at the user's request: `feat/remote-server-client`, `feat/torque-release-taper`, `fix/arm-identity-block-unverified`, `fix/camera-preview-mute-blank`, `fix/i9-inference-orphan-worker-bypasses-sibling-mutex`, `fix/recording-discard-resume-awareness`, `fix/teleop-record-restart-during-return`, `wip/ci-stacked-pr-checks`, `wip/cloud-stop-checkpoints`, and `backup/pr-24`, `backup/pr-26`, `backup/pr-28`, `backup/pr-29`. These were dormant, not established as fully merged; deletion was explicitly authorized after that distinction was reported.
- Removed redundant local branches `feat/drtc-s3.9-ui`, `refresh/studio-panels-rework-2`, `backup/rig-2026-09-04`, `staging-backup`, `staging-merged`, and later `refresh/robot-action-affordance-2`, after confirming their commits were preserved elsewhere. Removed origin `feat/drtc-s3.9-ui` and `feat/remote-teleop` after confirming containment in retained branches.
- Fast-forwarded local `feat/drtc-session` to `4fa7f09a`, matching origin and including the latest Run panel UI commit.
- Preserved and committed the camera work on `feat/camera-capture-mode`: `e5203a0a`, separating hardware capture mode from delivered frame size. The user elected to park its dependency publication issue; do not resume it without further direction.
- Preserved `feat/launcher-auto-remote` for a possible rescope. Its worktree still has uncommitted changes in `README.md`, the launcher script, `tests/test_scripts_makermodslab.py`, and `tests/test_sfu.py`.
- Verified Quirky Solomon's functional changes were already committed on `fix/policy-extras-pinned-fork` (open PR #117 at inspection). Discarded its remaining duplicate changes as requested.
- Verified Kind Lovelace's main MolmoAct2 implementation was already on `feat/molmoact2-rollout`, but base-model prefetch/progress and associated tests were unique. Incorporated that remainder into the feature branch as `17b2c3a9`; not pushed during this task.
- Removed both obsolete Claude worktrees/branches after preserving or explicitly discarding their changes. Removed the clean camera, Portal, and MolmoAct2 worktrees while retaining their branches and commits. Two MakerModsLab worktrees remained at last inspection: the main checkout and the launcher worktree. A separate LeRobot clone remains outside this worktree inventory.
- Retained open-PR branches and screenshot/demo branches. Local `staging` was 61 commits ahead and 1 behind origin; containment in local staging does not imply publication or PR merge.
- Updated `.agents/skills/deploy-agent/SKILL.md` to remove Opus-specific requirements and inherit the session model/settings by default. Corrected its description's angle-bracket validation issue. Broader proposed skill changes were not applied.

## Camera dependency: verified findings and blocker

- Existing core LeRobot pin: `eaab69339120787948776e4354dcee09f501fd16` in the makermods-robotics fork.
- All 10 initially failing camera backend checks were resolved when tested against the matching local LeRobot camera implementation. Committed that implementation in the separate clone on `feat/camera-capture-size` as `6cb1d3cbc2ded1d4043694a1d7c9aaded0c420cd`, directly above the existing pin.
- GitHub rejected publication with HTTP 403: the active account `Mokuroh54` lacked write access to `makermods-robotics/lerobot`. This was a GitHub authorization failure, not an automatic tool-approval rejection.
- MakerModsLab's dependency pin was not changed to the unpublished commit. A proposed pin/lockfile patch was prepared at `/private/tmp/camera-dependency-pin.patch`; temporary files are not durable project artifacts.
- The separate planned `0.2.5` bump is for `livekit-portal`, `lerobot-teleoperator-livekit`, and `lerobot-robot-livekit`. It does not update core LeRobot. `chore/portal-0.2.5-combined` retains that work at `7f657c39`, one commit ahead of origin `feat/remote-combined` at inspection.

## Validation performed

- Camera feature: 28 frontend tests passed and TypeScript check passed.
- With the old LeRobot pin: 735 backend checks passed, 10 failed.
- With the matching local LeRobot source on the import path: all 745 selected backend checks passed. This does not mean the unchanged default dependency installation is fixed.
- LeRobot OpenCV tests: 57 passed, 1 skipped; Ruff lint and format checks passed. Initial fixture-related failures were resolved by downloading/checking out the existing Git LFS camera images.
- Incorporated MolmoAct2 progress work: 368 selected tests passed.
- Updated deploy-agent skill passed the skill-creator validator.
- No live-robot experiment or training run was performed by this task.

## Extra model cameras: corrected finding

The user recalled support for adding camera inputs to variable-view models. Inspection confirmed this is already committed in local and origin `feat/drtc-session`: backend commit `8b807689` and frontend commit `efa598b0` are retained. The `--extra-image-roles` flow changes both model input features and the saved image pack processor, with an “Add a camera role” UI. Current allowlist: MolmoAct2 only; maximum two extra roles. Other model families require explicit support. This is separate from capture-resolution work and survived cleanup. An earlier explanation describing camera roles only as assignment was too narrow and was corrected. Improved model performance from extra views was not established.

## Proposals and next steps (not authorized background work)

- Leave the camera dependency issue parked per user direction. If resumed, obtain a writable publication destination before applying the prepared immutable pin.
- Await the user's launcher rescope; preserve its uncommitted work.
- Portal and MolmoAct2 local commits still need a user-directed publication/integration decision.
- Proposed skill improvements: scope delegation rules to delegated tasks; reconcile conflicting commit and file-ownership rules; remove stale paths/tool assumptions; allow routine implementation decisions; test affected behavior rather than only modified test files. Only model neutrality and the validation correction were applied.
- Future journal updates should accompany substantive user-directed work. No automation was created.

## MolmoAct2 startup loader inspection

- Instrumented GPU log `~/.cache/huggingface/lerobot/logs/drtc/gpu/1788584408.log` reports Tailscale startup 1.0 s, `policy.from_pretrained` 144.7 s, subsequent CUDA transfer 2.5 s, processors 1.8 s, and warmup 5.9 s. It confirms H100 and 5,442,196,208 parameter elements in BF16 on CUDA. The visible Transformers weights bar finishes in about 4 s.
- Inspected installed LeRobot at the same immutable SHA used by the Modal image (`eaab69339120787948776e4354dcee09f501fd16`). MolmoAct2 first loads its HF base model, then `_strict_load_safetensors_weights` reloads the checkpoint on CPU and copies it into the model; the inherited policy loader subsequently loads the LeRobot checkpoint. These silent additional weight passes are verified in source and a leading explanation for the post-bar delay. Individual pass durations, CPU contention, and storage/page-fault costs remain unmeasured; the full 144.7 s cannot yet be assigned to a particular pass. No loader behavior or resource allocation was changed for this inspection.

## E2E follow-up tests and correlated timing

- Added per-request policy callback-to-selection queue and selection-to-send service durations; robot replies now log a correlated RTT measured on the robot's monotonic clock. Added the read-only `python -m makermodslab.drtc.analyze_timing` utility, matching observation IDs before computing residuals and percentiles. It excludes ambiguous repeated IDs and warns about insufficient pairs or negative residuals. Removed an unsupported ±5 ms clock-agreement assumption from an existing comment.
- Expanded `docs/drtc/TIMING_DIAGNOSTICS.md` with baseline plus isolated slack 5→2, H264→MJPEG, and flow-steps 10→6 comparisons, exact UI/CLI locations, matched windows, and interpretation gates. Six is an existing UI option; five is CLI-only. Verified cached backbone default is 10 inference steps; the policy's training `num_flow_timesteps=8` is unrelated.
- Existing half-horizon estimator cap also drives request cooldown; this supports testing queue delay but does not prove backlog or justify changing RTC constraints. No E2E improvement is claimed yet. A new GPU worker and robot session are needed to collect the new timing records; no GPU or robot run was launched during this follow-up.
- Validation: 13 focused analyzer/diagnostic/RTC checks passed; Ruff lint/format, analyzer CLI help, and diff whitespace checks passed. Existing framework deprecation warnings remain unrelated. Analyzer tests cover per-ID subtraction, reordered/missing/duplicate records, warmup windows, malformed measurements, and negative residuals.

## Single-pass MolmoAct2 loader wired

- At the user's request, added `drtc/_policy_loading.py` and wired it into both RTC and sync policy entrypoints. Full Molmo checkpoints now construct the architecture on the meta device and strictly validate/assign the final LeRobot safetensors once into the requested dtype/device, skipping both base-weight passes. Metadata-only fetching and a saved-pack-processor override prevent base weights from being downloaded indirectly. Adapter configurations retain the existing loader. No installed dependency, immutable pin, GPU allocation, or robot session was changed.
- Preserved feature/norm-tag/action-mode/horizon validation and existing flow/precision/camera-role overrides; missing keys, unexpected keys, and shape mismatches fail before tensor assignment. Rebuild empty nonpersistent rotary caches and restore the persistent frequency-buffer alias after assignment. Documented implementation coupling to the current LeRobot pin.
- Validation: 81 focused tests passed, including the real tiny Molmo architecture's exact parameter/buffer and seeded action equivalence in FP32/BF16 with RTC both off and guided; tests forbid base-weight reads and assert each final tensor is read once. Both entrypoints were exercised with precision/flow/extra-camera overrides. Original framework deprecation warnings remain.
- Read only the actual HF checkpoint header (LeRobot revision `a93b5fcdce4c1e3cc7688c939fc943bf4b092a8a`) and matched all 1,295 tensor names/shapes against the full empty architecture built from cached base revision `152569fe57914d97be91055800035f54e250d009`: zero missing/unexpected/shape mismatches, 5,442,196,208 parameter elements, all unmaterialized. Empty architecture construction took 0.063 s locally; this is not a full loading benchmark. Full-size GPU timing/output verification remains for the next launch. No live speedup claim yet.

## Sync slack UI control

- Added a remembered “Sync slack (ticks)” selector offering 2/3/4/5 on the Modal launch card, default 5. Wired it through the start request, strict API/launcher validation, both Modal wrapper commands, status echo/reset, and settings-drift detection. The copied command and Start GPU use the same selection; controls are disabled during launch or while the GPU runs. Added English/Chinese labels and updated the E2E test guide.
- Default 5 still omits the flag and keeps the existing wrapper behavior; nondefault choices emit `--slack`. API clients may provide whole-number values from 2 through 30. Slack is a GPU launch setting, not a robot wire-schema field or a live update.
- Validation: 131 launcher cases covered successfully across the full run and the focused rerun of the 10 slack cases; 37 frontend component/command tests passed, including actual selection, persistence, command/request agreement, running-state lock, and stale-form detection. Application/Node TypeScript checks, Python lint/format, diff checks, and production build passed. Rebuilt the bundled UI; the existing bundle-size advisory remains. No live session or GPU was restarted by this task; normal-mode Lab restart is needed to load the changed backend.
